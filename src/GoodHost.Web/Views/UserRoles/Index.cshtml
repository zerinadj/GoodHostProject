@model IEnumerable<Projekat_1.Models.UserRole>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdUser)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Role)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdUser)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Role.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdURole">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdURole">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdURole">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
