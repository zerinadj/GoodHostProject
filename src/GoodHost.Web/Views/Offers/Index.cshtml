@model IEnumerable<Projekat_1.Models.Offer>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Place)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserRole)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Place.IdPlace)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rent.IdRent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserRole.IdURole)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdOffer">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdOffer">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdOffer">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
